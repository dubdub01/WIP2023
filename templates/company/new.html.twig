{% extends "base.html.twig" %}

{% block title %}Création d'une Company
{% endblock %}


{% block body %}



	<div class="container">
		<h1>Création d'un compte Company</h1>

		{{form_start(myform)}}
		{{form_widget(myform)}}
		<button type="submit" class="btn btn-success">Confirmez l'inscription</button>
		{{form_end(myform)}}
	</div>
{% endblock %}
